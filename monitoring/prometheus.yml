# Tells Prometheus (free, open-source) where to find our app's metrics.
global:
  scrape_interval: 5s

scrape_configs:
  - job_name: "semantic-cache-proxy"
    static_configs:
      - targets: ["cache-proxy:8000"]
