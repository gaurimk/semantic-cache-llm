# Auto-configures Grafana (free, open-source) to already know about
# Prometheus when it starts up, so you don't have to click through
# setup screens.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
